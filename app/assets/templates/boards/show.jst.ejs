<ul>
  <% board.lists().each(function(list) { %>
    <li><%= list.escape('name') %></li>
  <% }) %>
</ul>

<div class="new-list">
  <div class="new-list-button">Add a new list...</div>
  <form class="new-list-form display-off">
    <input type="text" name="list[name]">
  </form>
</div>